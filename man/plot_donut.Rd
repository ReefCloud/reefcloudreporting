% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_donut.R
\name{plot_donut}
\alias{plot_donut}
\title{Plot Donut for Benthic Cover (Defaults to HARD CORAL)}
\usage{
plot_donut(
  tier_id,
  year = NULL,
  depth = "shallow",
  cover_type = "hard coral",
  show_labels = TRUE,
  label_format = c("count", "percent"),
  donut_width = 0.6
)
}
\arguments{
\item{tier_id}{Character. ReefCloud tier ID to query.}

\item{year}{Numeric (optional). Survey year to filter. Defaults to the maximum year available.}

\item{depth}{Character. Depth category to filter (e.g., \code{"shallow"}, \code{"deep"} or \code{"none"})}

\item{cover_type}{Character. Benthic group to plot (e.g., \code{"hard coral"}, \code{"macroalgae"},
\code{"soft coral"}, \code{"turf algae"}, \code{"crustose coralline algae"}, \code{"other"}).
Default \code{"hard coral"}.}

\item{show_labels}{Logical. Show per-segment labels (percentages or counts). Default \code{TRUE}.}

\item{label_format}{Character. Label type: \code{"percent"} or \code{"count"}. Default \code{"count"}.}

\item{donut_width}{Numeric in (0, 1]. Ring thickness (e.g., \code{0.6}). Default \code{0.6}.}
}
\value{
A \code{ggplot} donut chart.
}
\description{
Retrieves survey compositions via \code{get_benthic_cover()}, filters to a requested
benthic \code{cover_type} (default \code{"HARD CORAL"}), classifies cover into ranges
using \code{add_cover_categories()}, and renders a whole-donut plot. Uses
\code{get_regional_summary()} to add region/site context for labels.
}
