% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_cover_comparison.R
\name{get_cover_comparison}
\alias{get_cover_comparison}
\title{Compare Two Years of Benthic Cover}
\usage{
get_cover_comparison(
  tier_id,
  years = NULL,
  cover_type = "hard coral",
  depth = "shallow"
)
}
\arguments{
\item{tier_id}{Character or numeric. Region/tier identifier used by your helpers.}

\item{years}{Optional numeric length-2 vector. If omitted, uses c(min(year), max(year)).}

\item{cover_type}{Character. One of "hard coral", "macroalgae", "soft coral",
"turf algae", "crustose coralline algae", "other". Default "hard coral".}

\item{depth}{Character. One of "shallow", "deep" or "none. Default "shallow".}
}
\value{
A tibble with the columns described above. Each \strong{site} that has
both years contributes \strong{one row}; the \strong{tier} contributes one row.
}
\description{
Creates a single tidy table containing \strong{tier} and \strong{site} rows for the
comparison between the \strong{first} and \strong{last} available years (by default),
adding absolute and percent change as columns.
}
\details{
Columns returned (only):
\itemize{
\item \code{tier_id}, \code{region_name}, \code{cover_type}, \code{depth}
\item \code{row_type}  ("Tier" OR "Site: <site_name>")
\item \code{year_first} (baseline year) and \code{year_last} (comparison year)
\item \code{n_sites}   (tier row: #sites in last year; site rows: 1)
\item \code{mean_cover}            (tier/site value in last year)
\item \code{diff_from_first}       (last - first)
\item \code{pct_change_from_first} (percent change vs first; NA if baseline is 0)
}
}
\examples{
\dontrun{
tab <- get_cover_comparison(1705)
tab_ma <- get_cover_comparison(1705, cover_type = "macroalgae", depth = "deep")
tab_custom <- get_cover_comparison(1705, years = c(2009, 2016))
}

}
