% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_cover_comparison_lr.R
\name{get_cover_comparison_lr}
\alias{get_cover_comparison_lr}
\title{Compare Two Years of Benthic Cover at Local Region Level (plus Tier)}
\usage{
get_cover_comparison_lr(
  tier_id,
  years = NULL,
  cover_type = "hard coral",
  depth = "shallow",
  local_region_col = "local_region"
)
}
\arguments{
\item{tier_id}{Character or numeric. Region/tier identifier used by your helpers.}

\item{years}{Optional numeric length-2 vector. If omitted, uses c(min(year), max(year)).}

\item{cover_type}{Character. One of "hard coral", "macroalgae", "soft coral",
"turf algae", "crustose coralline algae", "other". Default "hard coral".}

\item{depth}{Character. One of "shallow", "deep" or "none". Default "shallow".}

\item{local_region_col}{Character scalar. Name of the column in \code{xdf}
(after joining site metadata) that contains the Local Region label.
Default \code{"local_region"}.}
}
\value{
A tibble with the columns described above. Each \strong{local region} that
has both years contributes \strong{one row}; the \strong{tier} contributes one
row.
}
\description{
Creates a single tidy table containing a \strong{tier} row and \strong{local-region}
rows for the comparison between the \strong{first} and \strong{last} available years
(by default), adding absolute and percent change as columns.
}
\details{
Columns returned (only):
\itemize{
\item \code{tier_id}, \code{region_name}, \code{cover_type}, \code{depth}
\item \code{row_type}  ("Tier" OR "Local Region: \if{html}{\out{<label>}}")
\item \code{year_first} (baseline year) and \code{year_last} (comparison year)
\item \code{n_sites}   (tier row: #sites in last year; local-region rows: #sites in local region in last year)
\item \code{mean_cover}            (value in last year)
\item \code{diff_from_first}       (last - first)
\item \code{pct_change_from_first} (percent change vs first; NA if baseline is 0)
}
}
\examples{
\dontrun{
tab_lr <- get_cover_comparison_lr(1705)
tab_lr_ma <- get_cover_comparison_lr(1705, cover_type = "macroalgae", depth = "deep")
tab_lr_custom <- get_cover_comparison_lr(1705, years = c(2009, 2016))
# If your local region column is named differently:
tab_lr_named <- get_cover_comparison_lr(1705, local_region_col = "local_region_name")
}

}
